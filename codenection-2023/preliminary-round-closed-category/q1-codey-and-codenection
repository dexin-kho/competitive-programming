#include <bits/stdc++.h>
using namespace std;

#define REP(i, a, b) for (int i = a; i < b; i++)

void solve(int n) {
    REP(i, 0, n) cout << "I LOVE CODENECTION\n";
}

int main() {
    int n;
    cin >> n;
    solve(n);
}
